import Typed from "typed.js";

const typed = new Typed('.typed', {
    strings: [
        'conectamos contigo y tus clientes'

    ],
    typeSpeed: 90,
    startDelay: 300,
    backSpeed: 90,
    backDelay: 3000,
    loop: true,
    loopCount: false,
    showCursor: true,
    cursorChar: '|',
})